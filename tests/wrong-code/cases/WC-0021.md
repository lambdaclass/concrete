# WC-0021 — &mut borrow through a match arm

**Category:** miscompile (high-risk boundary, same surface as WC-0004)
**Status:** fixed
**Discovered:** 66d8736 adversarial sweep

## Symptom

No bug surfaced in this exact program. The case exists in the
corpus because match + borrow is exactly the surface that bit
WC-0004 (where arm-local enum-payload bindings leaked into the next
match's var-table). Different shape (this program uses
`borrow ... in 'region` + match scope), same neighborhood.

## Expected behavior

`tests/programs/adversarial/borrow/mut_borrow_through_match.con`
returns `100`.

## Regression command

`make test-wrong-code`.

## Historical context

Adversarial sweep from `66d8736`. Adjacent to WC-0004 — if a fix
to borrow lifetimes or match-arm var scoping breaks this program,
the regression is most likely a sibling of WC-0004's underlying
class.
