# WC-0020 — Option<Newtype> through nested match arms

**Category:** miscompile (high-risk boundary, related to older
newtype-in-enum-payload bug)
**Status:** fixed
**Discovered:** 66d8736 adversarial sweep

## Symptom

No bug surfaced in this exact program. Registered as coverage
because newtype-in-enum-payload was a real codegen bug recently
fixed (see CHANGELOG entry on validated wrappers + newtype layout in
SSA codegen). Option<Newtype> threaded through multiple match arms
with re-wrap exercises the same layout rule that bug touched.

## Expected behavior

`tests/programs/adversarial/newtype/in_option_chain.con` returns
`200`.

## Regression command

`make test-wrong-code`.

## Historical context

Adversarial sweep from `66d8736`. The repro stays in the corpus to
catch payload-layout regressions in `Concrete/Lower.lean` and
`Concrete/EmitSSA.lean`'s enum-payload path.
