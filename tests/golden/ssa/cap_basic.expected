; module main
declare i32 @putchar(i32)

; source: print_int @ line 3
define void @print_int(i64 %n) {
entry:
  %addr.1 = alloca %String
  %t0 = call %String @int_to_string(i64 %n)
  store %String %t0, %addr.1
  %t2 = call i64 @string_length(ptr(%String) %addr.1)
  br label %while.hdr0
while.hdr0:
  %phi.5 = phi i64 [0, %entry], [%t10, %while.body1]
  %t6 = slt i1 %phi.5, %t2
  br i1 %t6, label %while.body1, label %while.exit2
while.body1:
  %t7 = call i64 @string_char_at(ptr(%String) %addr.1, i64 %phi.5)
  %t8 = cast i64 %t7 to i32
  %t9 = call i32 @putchar(i32 %t8)
  %t10 = add i64 %phi.5, 1
  br label %while.hdr0
while.exit2:
  %t11 = call i32 @putchar(i32 10)
  %pload.12 = load %String, %addr.1
  call void @drop_string(%String %pload.12)
  ret void
}

; source: greet @ line 16
define void @greet() {
entry:
  call void @print_int(i64 42)
  ret void
}

; source: main @ line 20
define i64 @main() {
entry:
  call void @greet()
  ret i64 0
}
