struct Pair { a: Int, b: Int }

fn read_a(p: &Pair) -> Int {
    return p.a;
}

fn consume(p: Pair) -> Int { return p.a; }

fn main() -> Int {
    let p: Pair = Pair { a: 10, b: 20 };
    let val: Int = read_a(&p);
    let dummy: Int = consume(p);
    return val;
}
