enum Token { Num { value: Int }, Add { } }

fn eval(t: Token) -> Int {
    match t {
        Token::Num { value } => { return value; },
        Token::Add { } => { return 0; },
    }
}

fn main() with(Console) {
    let t: Token = Token::Num { value: 42 };
    println((eval(t)));
}
