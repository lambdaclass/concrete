struct Point {
    x: Int,
    y: Int,
}

fn sum_point(p: Point) -> Int {
    let Point { x, y } = p;
    return x + y;
}

fn main() with(Console) {
    let mut p: Point = Point { x: 1, y: 2 };
    p.x = p.x + 10;
    p.y = p.y + 20;
    println((sum_point(p)));
}
