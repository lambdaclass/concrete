// Loop control (negative): in a while-as-expression the `break` value type must
// match the `else` value type. Here break is bool, else is Int. Expect: E0222
mod m {
    fn main() -> Int {
        let x: Int = while true {
            break true;
        } else {
            42
        };
        return x;
    }
}
