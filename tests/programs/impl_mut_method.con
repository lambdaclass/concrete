struct A { a: Int, b: Int }

impl A {
    fn set_a(&mut self, value: Int) {
        self.a = value;
    }
}

fn consume_a(a: A) -> Int {
    let A { a, b } = a;
    return a;
}

fn main() with(Console) {
    let mut x: A = A { a: 2, b: 3 };
    x.set_a(42);
    println((consume_a(x)));
}
