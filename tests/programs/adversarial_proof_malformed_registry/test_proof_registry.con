// Adversarial test: malformed registry JSON
// The proof-registry.json next to this file is broken JSON.
// The system should handle it gracefully — no crash, no "proved" claim.

fn pure_add(a: Int, b: Int) -> Int {
    return a + b;
}

fn main() -> i32 {
    return 0;
}
