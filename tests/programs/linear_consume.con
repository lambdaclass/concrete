struct Resource {
    value: Int,
}

fn consume_resource(r: Resource) -> Int {
    let Resource { value } = r;
    return value;
}

fn main() with(Console) {
    let r: Resource = Resource { value: 42 };
    println((consume_resource(r)));
}
