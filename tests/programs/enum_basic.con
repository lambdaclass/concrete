enum Color { Red { }, Blue { } }

fn pick(c: Color) -> Int {
    match c {
        Color::Red { } => { return 1; },
        Color::Blue { } => { return 2; },
    }
}

fn main() with(Console) {
    let c: Color = Color::Blue { };
    println((pick(c)));
}
