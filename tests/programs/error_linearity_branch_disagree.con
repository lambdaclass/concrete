// Error: if/else where only one branch consumes a linear variable
struct Resource { value: Int }

fn consume(r: Resource) -> Int {
    return r.value;
}

fn main() -> Int {
    let r: Resource = Resource { value: 42 };
    if true {
        let x: Int = consume(r);
    } else {
    }
    return 0;
}
