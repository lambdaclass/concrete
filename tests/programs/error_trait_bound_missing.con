trait Describe {
    fn describe(&self) -> Int;
}

struct Point { x: Int, y: Int }

fn show<T: Describe>(x: Int) -> Int {
    return x;
}

fn consume_point(p: Point) -> Int {
    return p.x;
}

fn main() -> Int {
    let result: Int = show::<Point>(42);
    return result;
}
