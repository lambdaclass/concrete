fn main() -> Int {
    let a: String = "hello";
    let b: String = "world";
    let eq: Bool = string_eq(&a, &b);
    drop_string(a);
    drop_string(b);
    if eq { return 1; } else { return 0; }
}
