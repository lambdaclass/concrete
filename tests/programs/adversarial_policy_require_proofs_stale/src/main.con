// Adversarial: require-proofs = true, but registry fingerprint is stale.
// Policy layer must reject this with E0612 (stale proof).

fn pure_add(a: Int, b: Int) -> Int {
    return a + b;
}

pub fn main() -> Int {
    return pure_add(1, 2);
}
