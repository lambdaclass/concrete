// Test: proof registry with wrong function name (should not prove anything)
fn pure_add(a: Int, b: Int) -> Int {
    return a + b;
}

fn main() -> i32 {
    let _x: Int = pure_add(1, 2);
    return 0;
}
