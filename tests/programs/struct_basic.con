struct Point {
    x: Int,
    y: Int,
}

fn sum_point(p: Point) -> Int {
    let Point { x, y } = p;
    return x + y;
}

fn main() with(Console) {
    let p: Point = Point { x: 3, y: 4 };
    println((sum_point(p)));
}
