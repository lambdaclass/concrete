// Error: even inside trusted fn, extern call requires with(Unsafe)
extern fn puts(s: *const u8) -> i32;

trusted fn call_extern(s: *const u8) -> i32 {
    return puts(s);
}

fn main() -> i32 {
    return 0;
}
