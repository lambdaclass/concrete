// module main
enum Option<T> {
  Some { value: T },
  None,
}

enum Result<T, E> {
  Ok { value: T },
  Err { value: E },
}

fn fib(n: Int) -> Int {
  if (n <= 1) {
    return n;
  }
  return (fib((n - 1)) + fib((n - 2)));
}

fn main() -> Int {
  return fib(10);
}
