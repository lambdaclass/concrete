mod project {
    mod other;
    mod mymod;

    import other.{hello};
    import mymod.submodule.{test};

    pub fn main() -> u32 {
        let y: u32 = 2;
        return hello(8) + test();
    }
}
