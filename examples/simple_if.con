mod Simple {
    fn main() -> bool {
        let y: bool = check(0);
        return y;
    }

    fn check(x: i64) -> bool {
        let y: i64 = 0;
        if x == y {
            return true;
        }
        return false;
    }
}
