# This is a TOML test file

title = "TOML Example"
version = 42
enabled = true
debug = false

# Array examples
ports = [8080, 8081, 8082]
tags = ["web", "server", "concrete"]
mixed = [1, 2, 3]

# Inline table
point = {x = 10, y = 20}

[server]
host = "0.0.0.0"
port = 443
ssl = true

[database]
name = "mydb"
max_connections = 100
enabled = true
hosts = ["primary.example.com", "replica.example.com"]

[owner]
name = 'Tom Preston-Werner'
bio = "GitHub founder\nLikes tater tots"
