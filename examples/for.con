mod Example {
    fn main() -> i64 {
        return sum_to(4);
    }

    fn sum_to(limit: i64) -> i64 {
        let mut result: i64 = 0;

        for (let n: i64 = 1; n <= limit; n = n + 1) {
            result = result + n;
        }

        return result;
    }
}
