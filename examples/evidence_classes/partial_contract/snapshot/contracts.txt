error: proof link for 'demo.ct_compare' has no stored proof subject — #[proof_by] without #[proof_fingerprint] cannot detect a changed body (the freshness check would compare the current body with itself), so this claim is unbound, not proved. Add #[proof_fingerprint("...")] after re-verifying
=== Source Contracts ===

demo.ct_compare
  O1  ensures (a == b && result == 1) || (a != b && result == 0)
     status:  partial — one direction proved_by_lean, converse outstanding
     theorem: Examples.ConstantTimeTag.Proofs.ct_compare_same_tag_correct  (coverage: one_direction)
     note:    full postcondition not yet discharged; the converse is the next obligation


=== Runtime-safety obligations (array bounds) ===

demo.ct_compare
  a[i]  (array size 16)
    status: unproven — bound the index with a #[requires], or insert a runtime check
  b[i]  (array size 16)
    status: unproven — bound the index with a #[requires], or insert a runtime check

