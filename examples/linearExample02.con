mod LinearExampleStub {

	struct Linear {
	    x: i32,
	    y: i32,
	}
	
	fn main() -> i32 {
		let xy: Linear = 
                    Linear {
                        x: 0,
                        y: 1,
                    };
          // FIXME Prefered initialization but not yet implemented
          // [1, 0];
		let sum: i32 = xy.x + xy.y;
		// linear value is written/consumed
		consume_x(&xy);
		return xy.x;
	}
	
	fn consume_x(value: &Linear) {
		value.x = value.x + 1;
	}
	
}