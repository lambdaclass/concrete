error: proof link for 'constant_time_tag.ct_compare' has no stored proof subject — #[proof_by] without #[proof_fingerprint] cannot detect a changed body (the freshness check would compare the current body with itself), so this claim is unbound, not proved. Add #[proof_fingerprint("...")] after re-verifying
=== Proof Status Report ===

-- proof link unbound ------------------------------------- examples/constant_time_tag/src/main.con:48

  proof link unbound: no stored proof-subject digest for `constant_time_tag.ct_compare`.

 48 |     fn ct_compare(a: [u8; 16], b: [u8; 16]) -> i32 {
    | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

  current fingerprint:
    [(let diff (int 0)) (let i (int 0)) (while (binop Concrete.BinOp.lt (var i) (int 16)) [(set diff (binop Concrete.BinOp.bitor (var diff) (binop Concrete.BinOp.bitxor (index (var a) (var i)) (index (var b) (var i))))) (set i (binop Concrete.BinOp.add (var i) (int 1)))] [(set i (binop Concrete.BinOp.add (var i) (int 1)))]) (if (binop Concrete.BinOp.eq (var diff) (int 0)) [(ret (int 1))]) (ret (int 0))]

  Not proved, and not stale: the body has not been shown to change, it has
  never been pinned. With no stored subject the freshness check would compare
  this body against itself.

  coverage: iff

  origin: source_linked

  spec: drift-checked (Concrete.Proof.specs)

  hint: Re-verify the proof against the current body, then record the result as #[proof_fingerprint("...")].

-- not eligible ------------------------------------------- examples/constant_time_tag/src/main.con:67

  `constant_time_tag.main` cannot be proved: fails predictable profile (has capabilities: Console, is entry point (main)).

 67 |     pub fn main() with(Console) -> i32 {
    | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

  reasons: has capabilities: Console, is entry point (main)

  hint: Address these constraints to make this function eligible for proof.

Totals: 2 functions — 0 proved, 0 stale, 1 unbound, 0 unproved, 0 blocked, 1 ineligible, 0 trusted

