enum Color { Red { }, Blue { }, Green { } }

fn pick(c: Color) -> Int {
    match c {
        Color#Red { } => { return 1; },
        Color#Blue { } => { return 2; },
    }
}

fn main() -> Int {
    let c: Color = Color#Red { };
    return pick(c);
}
