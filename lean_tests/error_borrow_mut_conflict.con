// Error: mutable and immutable borrow conflict
// Note: This tests nested borrow blocks on the same variable
fn main() -> Int {
    let mut x: Int = 10;
    borrow x as r1 in R1 {
        borrow mut x as r2 in R2 {
            *r2 = 42;
        }
    }
    return x;
}
