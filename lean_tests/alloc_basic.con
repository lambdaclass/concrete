// Test: basic heap allocation, arrow access, and free
struct Point { x: Int, y: Int }

fn main() with(Std) -> Int {
    let p: Heap<Point> = alloc(Point { x: 10, y: 20 });
    let result: Int = p->x + p->y;
    free(p);
    return result;
}
